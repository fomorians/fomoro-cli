#!/usr/bin/env node

var commander = require('commander');
var session_stop = require('../lib/handlers/session_stop');

commander
  .description('Stop a training session.')
  .arguments('[key]')
  .parse(process.argv);

session_stop.apply(null, commander.args);
