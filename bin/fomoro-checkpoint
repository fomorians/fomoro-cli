#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage checkpoints.')
  .arguments('<subcommand>')
  .command('pull [key]', 'Pull training session checkpoints.')
  .command('rm [key]', 'Remove training session checkpoints.')
  .parse(process.argv);
