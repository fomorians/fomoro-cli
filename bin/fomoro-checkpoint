#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage checkpoints.')
  .arguments('<subcommand>')
  .command('pull [sha]', 'Pull training session checkpoints.')
  .command('rm [sha]', 'Remove training session checkpoints.')
  .parse(process.argv);
