#!/usr/bin/env node

var commander = require('commander');
var session_start = require('../lib/handlers/session_start');

commander
  .description('Start a training session.')
  .arguments('[sha]')
  .parse(process.argv);

session_start.apply(null, commander.args);
