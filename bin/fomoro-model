#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage models.')
  .arguments('<subcommand>')
  .command('list', 'List models.')
  .command('push', 'Push a new model.')
  .command('pull', 'Pull an existing model.')
  .command('rm', 'Remove a model.')
  .parse(process.argv);
