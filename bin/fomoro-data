#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage datasets.')
  .arguments('<subcommand>')
  .command('list', 'List datasets.')
  .command('push', 'Push a new dataset.')
  .command('rm', 'Remove a dataset.')
  .parse(process.argv);
