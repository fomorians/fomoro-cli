#!/usr/bin/env node

var commander = require('commander');
var session_info = require('../../lib/handlers/session/session_status');

commander
  .description('Print the status of a training session.')
  .arguments('[sha]')
  .parse(process.argv);

session_info.apply(null, commander.args);
