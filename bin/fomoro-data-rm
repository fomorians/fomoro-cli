#!/usr/bin/env node

var commander = require('commander');
var data_rm = require('../lib/handlers/data_rm');

commander
  .description('Remove a dataset.')
  .parse(process.argv);

data_rm.apply(null, commander.args);
