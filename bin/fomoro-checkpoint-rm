#!/usr/bin/env node

var commander = require('commander');
var checkpoint_rm = require('../lib/handlers/checkpoint_rm');

commander
  .description('Remove training session checkpoints.')
  .parse(process.argv);

checkpoint_rm.apply(null, commander.args);
