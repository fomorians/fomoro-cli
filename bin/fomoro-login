#!/usr/bin/env node

var commander = require('commander');
var login = require('../lib/handlers/login');

commander
  .description('Login to Fomoro.')
  .parse(process.argv);

login.apply(null, commander.args);
