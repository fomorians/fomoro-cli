#!/usr/bin/env node

var commander = require('commander');
var session_list = require('../lib/handlers/session_list');

commander
  .description('List all training sessions.')
  .parse(process.argv);

session_list.apply(null, commander.args);
