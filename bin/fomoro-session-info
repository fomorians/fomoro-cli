#!/usr/bin/env node

var commander = require('commander');
var session_info = require('../lib/handlers/session_info');

commander
  .description('Inspect a training session.')
  .arguments('[key]')
  .parse(process.argv);

session_info.apply(null, commander.args);
