#!/usr/bin/env node

var commander = require('commander');
var summary_rm = require('../lib/handlers/summary_rm');

commander
  .description('Remove training session summary logs.')
  .arguments('[key]')
  .parse(process.argv);

summary_rm.apply(null, commander.args);
