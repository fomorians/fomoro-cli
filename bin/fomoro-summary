#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage summary logs.')
  .arguments('<subcommand>')
  .command('pull', 'Pull training session summary logs.')
  .command('rm', 'Remove training session summary logs.')
  .parse(process.argv);
