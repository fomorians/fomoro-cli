#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage summary logs.')
  .arguments('<subcommand>')
  .command('pull [sha]', 'Pull training session summary logs.')
  .command('rm [sha]', 'Remove training session summary logs.')
  .parse(process.argv);
