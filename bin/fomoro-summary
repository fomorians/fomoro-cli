#!/usr/bin/env node

var commander = require('commander');

commander
  .description('Manage summary logs.')
  .arguments('<subcommand>')
  .command('pull [key]', 'Pull training session summary logs.')
  .command('rm [key]', 'Remove training session summary logs.')
  .parse(process.argv);
