#!/usr/bin/env node

var commander = require('commander');
var model_push = require('../lib/handlers/model_push');

commander
  .description('Push a new model.')
  .parse(process.argv);

model_push.apply(null, commander.args);
