#!/usr/bin/env node

var commander = require('commander');
var data_push = require('../lib/handlers/data_push');

commander
  .description('Push a new dataset.')
  .parse(process.argv);

data_push.apply(null, commander.args);
