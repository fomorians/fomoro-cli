#!/usr/bin/env node

var commander = require('commander');
var summary_pull = require('../lib/handlers/summary_pull');

commander
  .description('Pull training session summary logs.')
  .arguments('[sha]')
  .parse(process.argv);

summary_pull.apply(null, commander.args);
