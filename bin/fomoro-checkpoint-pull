#!/usr/bin/env node

var commander = require('commander');
var checkpoint_pull = require('../lib/handlers/checkpoint_pull');

commander
  .description('Pull training session checkpoints.')
  .arguments('[sha]')
  .parse(process.argv);

checkpoint_pull.apply(null, commander.args);
